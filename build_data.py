from pathlib import Path\nimport sqlite3\nimport sys\n\nROOT = Path(__file__).resolve().parents[1]\nsys.path.insert(0, str(ROOT))\n\nfrom build_emr_showcase import ensure_dirs, create_schema, build_dataset, export_tables, DB_PATH\n\ndef rebuild_showcase():\n    ensure_dirs()\n    conn = sqlite3.connect(DB_PATH)\n    create_schema(conn)\n    build_dataset(conn)\n    export_tables(conn)\n    conn.close()\n